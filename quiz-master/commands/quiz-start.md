---
name: quiz-start
description: 开始答题会话，按智能/随机/顺序策略提问
allowed-tools:
  - Read
  - Write
  - Bash
argument-hint: <bank-name> [--count N] [--mode smart|random|sequential]
---

# Quiz Master - Start Quiz Session

你正在帮助用户开始一个答题会话。

## 任务步骤

### 1. 读取题库文件

从 `~/.quiz-master/banks/{bank-name}.json` 读取题库：
- 使用 Read 工具读取题库文件
- 验证题库存在且格式正确
- 显示题库基本信息：名称、描述、题目总数

如果题库不存在：
```
❌ 找不到题库：{bank-name}

可用题库：
  - {bank1} ({count} 题)
  - {bank2} ({count} 题)

提示：使用 /quiz-import 导入新题库，或使用 /quiz-list 查看所有题库
```

### 2. 解析参数

解析用户提供的参数：
- `bank-name`: 题库名称（必需）
- `--count N`: 题目数量（可选，默认从配置读取或10题）
- `--mode smart|random|sequential`: 提问模式（可选，默认smart）

### 3. 选择题目策略

根据模式选择题目：

**Smart 模式（智能推荐）**:
- 使用 quiz-learning-analytics skill 的算法
- 优先选择错误次数多的题目
- 优先选择长时间未复习的题目
- 避免连续出现同一知识点的题目
- 权重公式：`score = (错误次数 × 2) + (距上次答题天数 × 0.5)`

**Random 模式（随机）**:
- 从题库中随机抽取指定数量的题目
- 使用 Fisher-Yates 洗牌算法确保随机性

**Sequential 模式（顺序）**:
- 按题目 ID 或数组顺序依次出题
- 从上次停止的位置继续（如果支持进度保存）

### 4. 检查学习历史（Smart 模式）

如果使用 smart 模式，读取学习日志：
- 解析 `~/.quiz-master/logs/session-*.log` 文件
- 统计每个题目的错误次数和上次答题时间
- 计算题目优先级权重

### 5. 开始交互式答题

采用逐题回答模式：

**显示题目**:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📝 题目 {current}/{total}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{question_content}

{options_display}

💡 提示：输入你的答案（或输入 'skip' 跳过，'quit' 退出）
```

**等待用户回答后**:
- 记录答题时间
- 判断答案是否正确
- 显示反馈信息

**显示反馈**:
```
✅ 回答正确！或 ❌ 回答错误

正确答案：{correct_answer}
{explanation}

⏱️  用时：{time} 秒
📊 你的答案：{user_answer}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

按 Enter 继续下一题...
```

### 6. 记录答题数据

实时记录到日志文件 `~/.quiz-master/logs/session-{timestamp}.log`：
```
[{timestamp}] Question {id}: "{question}" - {Correct|Incorrect} ({time}s)
  - User Answer: {user_answer}
  - Correct Answer: {correct_answer}
```

### 7. 会话结束

当完成所有题目或用户退出时：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 答题会话结束
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

题库：{bank-name}
模式：{mode}
题目总数：{total}
已完成：{completed}
跳过：{skipped}

📈 成绩统计：
  ✅ 正确：{correct} ({correct_rate}%)
  ❌ 错误：{incorrect}
  ⏭️  跳过：{skipped}

⏱️  总用时：{total_time} 秒
⚡ 平均每题：{avg_time} 秒

💪 加油！继续使用 /quiz-analyze 查看薄弱知识点

日志已保存：~/.quiz-master/logs/session-{timestamp}.log
```

### 8. 触发学习分析

会话结束后，主动调用 learning-analyzer agent：
```
本次答题会话已完成，请分析我的学习数据并提供改进建议。
```

## 参数说明

**必需参数**:
- `bank-name`: 题库名称（不含 .json 扩展名）

**可选参数**:
- `--count N`: 本次答题的题目数量（默认：10 或从配置读取）
- `--mode smart|random|sequential`: 提问策略
  - `smart`: 智能推荐优先练习薄弱题目（默认）
  - `random`: 随机出题
  - `sequential`: 顺序出题

## 使用示例

**开始智能模式答题（默认10题）**:
```bash
/quiz-start english-vocab
```

**指定题目数量**:
```bash
/quiz-start math-problems --count 20
```

**使用随机模式**:
```bash
/quiz-start history-quiz --mode random --count 15
```

**顺序模式复习**:
```bash
/quiz-start programming-basics --mode sequential
```

## 交互命令

答题过程中用户可以输入：
- `skip` - 跳过当前题目
- `quit` 或 `exit` - 退出答题会话
- `hint` - 显示提示（如果题库提供）
- 直接输入答案作答

## 错误处理

**题库不存在**:
```
❌ 题库不存在：{bank-name}

使用 /quiz-list 查看可用题库
使用 /quiz-import 导入新题库
```

**题目数量超出范围**:
```
⚠️  请求的题目数量（{count}）超过题库总数（{total}）
将调整为 {total} 题
```

**日志目录创建失败**:
```
⚠️  无法创建日志目录，答题数据将不会被保存
是否继续？(y/n)
```

## 配置影响

从 `.claude/quiz-master.local.md` 读取配置：
- `default_strategy`: 默认提问模式
- `questions_per_session`: 默认题目数量
- `show_hints`: 是否显示提示
- `auto_save_interval`: 自动保存间隔

## 相关技能

- `quiz-learning-analytics`: 提供智能推荐算法
- `learning-analyzer` agent: 学习数据分析

## 相关命令

- `/quiz-import`: 导入题库
- `/quiz-status`: 查看学习进度
- `/quiz-analyze`: 分析薄弱知识点
- `/quiz-list`: 管理题库
